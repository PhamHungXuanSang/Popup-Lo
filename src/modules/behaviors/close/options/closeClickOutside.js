"use strict";

const closeClickOutSide = (config, keyPopup) => {
  // Get the popup element
//   const elementPopup = document.querySelector(`.${keyPopup}`);

//   // Add an event listener for clicks on the popup element
//   elementPopup.closest('.saas-popup').addEventListener("click", (e) => {
//     // Prevent the default behavior (e.g., following a link)
//     e.preventDefault();

//     // Call a function to close the popup
//     closePopup(elementPopup);

//     // Stop the event from propagating if the event target is not a child of the popup element
//     if (!elementPopup.contains(e.target)) {
//       e.stopPropagation();
//     }
//   });
};

// Function to close the popup
// function closePopup(element) {
//   // Remove the `active` class from the popup element
//   element.querySelector('.closeClickOutSide').classList.remove("active");
// }

export default closeClickOutSide;
